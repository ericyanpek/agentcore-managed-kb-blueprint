[pytest]
# Put the repository root on sys.path so tests import the kbp package in place.
# Without this, the __init__.py files under tests/ make pytest resolve test
# modules as a package and the root never lands on sys.path.
pythonpath = .
testpaths = tests
